fn main() {
    var x: int? = nil;
    x = |10|;
    x; // int?

    let a = <int?> |10|;
    a; // int?

    // ---

    let n: (float?)? = |nil|; // error (correct type: float??)
    let b = |nil|;
}
